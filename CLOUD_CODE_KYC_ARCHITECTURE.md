# Android 移动端实名认证（KYC）技术架构与数据校验规范

本文件详述了 Android 移动端在标准合规要求下集成实名认证（KYC）的技术方案、数据采集字段规范、后台权威数据库核验流程，以及与设备/网络风控系统的协同机制。本方案供系统集成开发与合规性审计参考。

---

## 一、 移动端 KYC 接入路径对比

移动应用在集成身份核验时，通常根据安全等级与开发成本选择以下两种技术路径：

```
                ┌──────────────────────────────┐
                │        KYC 接入路径选择       │
                └──────────────┬───────────────┘
     ┌─────────────────────────┴─────────────────────────┐
     ▼                                                   ▼
[原生 SDK 模式 (如 Stripe Identity)]              [WebView H5 模式]
- 本地边缘计算 (OCR 实时边缘裁切)                   - H5 调用 WebRTC 摄像头
- 活体 3D 点云与面部防重放                         - 本地校验弱，依赖服务端审核
- 硬件级安全隔离 (TEE 签名校验)                     - 存在媒体流劫持 (Virtual Camera) 风险
```

### 1.1 原生 SDK 模式
* **数据流安全**：SDK 在独立的 Activity 中运行，采集的图像和视频在本地通过 TEE 进行硬件级加密签名后直接上传给合规服务商（如 Persona/Jumio），App 宿主无法拦截或篡改多媒体流。
* **本地 OCR 与边缘计算**：在本地自动识别证件边框，检测反光与模糊度，达标后自动抓拍，极大过滤了“拍摄照片照片”的欺诈行为。

### 1.2 WebView / H5 容器模式
* **数据流安全**：依赖浏览器的 `getUserMedia` 权限。若 Android 系统被 Root 或使用了虚拟摄像头挂钩（Hook），媒体输入流存在被篡改拦截的风险，整体安全等级较低。

---

## 二、 核心数据字段与核验物料规范

标准合规（特别是美国地区金融级合规）要求收集并校验以下多维度物料：

### 2.1 身份元数据字段 (Demographics)
| 字段名称 | 校验要求 | 作用说明 |
| :--- | :--- | :--- |
| **Full Name** | 必须与法定证件文字完全一致（含中间名 Middle Name）。 | 基础身份检索键值。 |
| **Date of Birth (DOB)** | 格式统一为 `YYYY-MM-DD`。 | 排除同名同姓的二次检索键。 |
| **Tax ID / SSN** | 美国地区要求输入全 9 位或后 4 位社会安全号。 | 税务与信用系统唯一索引。 |
| **Residential Address** | 必须是真实的物理住宅地址，禁止填写 P.O. Box（邮政信箱）。 | 用于匹配信用历史记录。 |

### 2.2 证件图像物料 (Document Images)
* **驾照/身份证 (Driver's License / State ID)**：要求上传正反双面，必须包含完整的 PDF417 双维条形码。
* **护照 (Passport)**：要求上传个人信息页，必须包含完整的机读区（MRZ，Machine Readable Zone）字符。

### 2.3 活体检测视频 (Liveness Biometrics)
* **动态自拍视频**：系统生成随机动作指令（如眨眼、向左看、抬头），提取脸部 128 个特征点（Face Landmarks），用以防范面具、屏幕重放及深度伪造（Deepfake）。

---

## 三、 后台核验与数据对比机制

当用户在移动端提交 KYC 物料后，合规系统在云端启动异步核验链条：

```mermaid
graph TD
    A[客户端提交 KYC 数据] --> B[合规网关审核]
    
    B --> C[权威数据库比对 (Bureau Check)]
    B --> D[全球制裁名单筛查 (AML/OFAC)]
    B --> E[生物特征比对 (Face Match)]
    
    C -- 校验 Name+DOB+SSN+Address --> C1{信用局数据是否存在?}
    C1 -- 存在且匹配 --> F[数据一致性通过]
    C1 -- 冲突/死号/无记录 --> G[拒绝验证/拒签]
    
    D -- 扫描 --> D1{是否在制裁/洗钱名单?}
    D1 -- 是 --> G
    D1 -- 否 --> H[声誉合规通过]
    
    E -- 提取 3D 脸部特征向量 --> E1{自拍与证件照匹配度 > 95%?}
    E1 -- 是 --> I[活体比对通过]
    E1 -- 否 --> G
    
    F --> J[综合决策引擎]
    H --> J
    I --> J
    
    J -- 全部通过 --> K[KYC 验证成功]
    J -- 任意项失败 --> G
```

### 3.1 信用局数据对齐（Bureau Checks）
在合规系统中，输入的 SSN、姓名和地址会被直接发送给美国三大信用局（Experian, TransUnion, Equifax）或金融数据库。系统验证该 SSN 的真实性、其对应的人士是否在世，以及该名字与该 SSN 的绑定关系是否在过去存在真实的信用卡申请、贷款或公共事业缴费历史。

---

## 四、 设备级与网络级协同风控

除了身份信息本身，风控系统还会将 KYC 数据与底层的设备和网络指纹进行**防关联比对**：

1. **IP 属地与账单国别关联**：
   * 提交美国身份（SSN / 账单地址）时，发起请求的代理 IP 的落地国必须同样为美国（US），且 IPQS 欺诈得分需在安全值内。
2. **设备指纹历史追踪**：
   * 原生 SDK 采集的 Android ID、GSF ID、硬件序列号等，会与该身份进行终身绑定。如果一个物理设备频繁更换不同的 Google 账号提交不同的实名 KYC，该设备指纹会被永久拉黑。
3. **系统环境可信度评估**：
   * 检测设备是否通过了 Google Play Integrity 的 `MEETS_DEVICE_INTEGRITY` 校验。解开 Bootloader 或被检测到 Root 的设备，在提交敏感金融 KYC 时会被自动判定为高危交易，直接转入人工二次审核。
